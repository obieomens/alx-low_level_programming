#include "main.h"
/**
 * mul - function that multiply two numbers
 * @a: first parameter
 * @b: second parameter
 * Return: value
 */
int mul(int a, int b)
{	int result;
	result = a * b;
	return (result);
}
